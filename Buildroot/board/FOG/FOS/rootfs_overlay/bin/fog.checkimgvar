#!/bin/bash
msg="Checking img variable is set"
dots $msg
if [[ -z $img ]]; then
    msg_val="Failed"
    echo "$msg_val"
    debugPause
    handleError "Fatal Error: img variable not set. ($0)\n   Args Passed: $*"
fi
msg_val="Done"
echo "$msg_val"
callBackLog $msg_val $msg
debugPause
